# BGM

`placeholder-pad.wav` は `scripts/dev/make-placeholder-bgm.ts` が生成した**仮の音**。
音量バランスを確認するためだけに置いてある。

本番の音源に差し替えること。調達方法（Suno / ロイヤリティフリー）は plan.md の未決事項。

差し替えるときの条件:

- **TikTok の楽曲ライブラリは使わない。** 媒体ごとに楽曲ライセンス契約が違うので、
  他媒体でライセンスが通らない
- 3媒体で同じ音源を使う（同一マスターから書き出すため）
- 小さいファイルだけ git 管理する
